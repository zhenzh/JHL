#if {$cols >= 170} {
    #split 0 1 0 -100 1;
};
#else {
    #split 0 0 0 0 1;
}

#config {mouse} {on};
#event {SCROLLED MOUSE WHEEL UP} {#buffer up 1};
#event {SCROLLED MOUSE WHEEL DOWN} {#buffer down 2};
